import 'package:flutter/material.dart';

class ExcersiceWidget extends StatelessWidget {
  const ExcersiceWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Excersice"),),
      body: Container(
        child: Column(
         children: [

         ],
        ),
      )
    );
  }
}
